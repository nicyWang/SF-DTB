# 交接文档 — 桌面AI搭档项目（给接手的模型/开发者）

## 项目状态总览

**项目路径**：`/Users/mac/WorkBuddy/2026-08-19-19-23-44/desktop-pet/`

### ✅ 已完成且验收通过（v1完全体）
- Electron透明窗桌宠（Live2D→sprite写实立绘渲染）
- LLM对话（OpenAI兼容API可配+mock模式）
- 三层记忆+性格演化（独立双档案）
- 屏幕感知/直播弹幕/角色工坊（用户自建角色：照片转绘+AI生成）
- 语音对话原型（TTS+按住说话）
- 全量测试：6个测试文件247断言+E2E全过
- 启动：`npm start`；自检：`PET_SELFTEST=1 npm start`

### ⚠️ 半成品（汪总不满意，需要重做方向）
**视频版桌宠demo**（`demo-v2.html` + `src/core/videoSprite.js`）：
- 技术链路已通：CDP实测视频切换/播放/GPU渲染全正常
- **核心问题：素材不行**——4段AI视频表情差异太小（prompt太温和），视觉上"看不出变化"
- 汪总体验：点按钮"没反应"（实际是视频内容几乎一样）

## 汪总的真实需求（重要！）
1. **形象**：真实感强的帅哥+美女双角色（"真实性感"，偶像级颜值）
2. **表现力**：真实表情+动作（对静态立绘+程序化滤镜强烈不满："目前啥也不是"）
3. **终极形态**：实时对话3D数字人（原话："现在不是有很多实时对话的3D数字人吗，直接接入"）
4. **交互**：idle时跟随鼠标，高光时刻播视频，成长解锁新动作视频

## 建议方向（按优先级）
1. **接云端数字人API**（最符合需求）：硅基智能DUIX（0.5元/分钟，端侧渲染120ms，原生透明视图）或百度曦灵（2元/分钟）——实时口型+表情+动作，这是汪总要的效果，自研拼凑达不到
2. 素材重做（若继续视频路线）：VideoGen prompt必须"动作幅度大、表情极端差异化"（如sad=低头闭眼嘴角下撇 vs happy=咧嘴大笑眼睛弯成月牙）
3. LiveTalking本地部署（需N卡GPU，Mac不可行）

## 今天的bug修复记录（都有价值，别回退）
- `src/core/videoSprite.js`：ES module导出缺失、视频路径`src/`前缀404、8视频并发解码器耗尽（去重到4个）、PIXI texture.update()缺失（画面冻结根因）、mask坐标系错位、seek+play并发死锁
- `main/main.js`：PET_VIDEO_DEMO=1分支（loadFile加载demo-v2.html）
- 验证方法：`--remote-debugging-port=9222` + CDP WebSocket脚本（/tmp/cdp-test.mjs模式，真实点击+GPU hash对比）

## 素材资产
- `src/assets/characters/handsome_v2|v3|v4/`：三版帅哥立绘（温润/高冷/阳光）+v2的4段视频（mp4+webm）
- `src/assets/characters/beauty_v2/`：美女立绘

## 关键教训
1. **交付前必须自己看到真实效果**（CDP/截图验证），绝不让用户当QA
2. 用户说"不行"时先问清楚"具体哪里不行"，不要猜
3. 需求理解要确认到位再动手，方向错了执行力越强浪费越大
